import { BoolTypeEvalPipe } from './bool-type-eval.pipe';

describe('BoolTypeEvalPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolTypeEvalPipe();
    expect(pipe).toBeTruthy();
  });
});

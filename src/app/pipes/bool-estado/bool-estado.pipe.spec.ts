import { BoolEstadoPipe } from './bool-estado.pipe';

describe('BoolEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});

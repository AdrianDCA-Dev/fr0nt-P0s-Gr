import { HoraIgualDirective } from './hora-igual.directive';

describe('HoraIgualDirective', () => {
  it('should create an instance', () => {
    const directive = new HoraIgualDirective();
    expect(directive).toBeTruthy();
  });
});

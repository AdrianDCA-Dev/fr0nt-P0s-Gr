import { EstadoBoolDirective } from './estado-bool.directive';

describe('EstadoBoolDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoBoolDirective();
    expect(directive).toBeTruthy();
  });
});
